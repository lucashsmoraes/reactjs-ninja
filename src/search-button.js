'use stric'

import React from 'react'
import Button from './button'

const SearchButton = () => (
  <Button handleClick={() => alert('SearchButton')}>
    Search
  </Button>
)

export default SearchButton
